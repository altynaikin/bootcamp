

function filterRange(x, y) {
    let arr = [];
    for(let i = x; i <= y; i++) {
            arr.push(i);
        }
        console.log(arr)
        return arr
    }

filterRange(1,100);
filterRange(6, 80);